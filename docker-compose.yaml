version: '3'

services:
  faucet:
    image: kinecosystem/stellar-faucet:0.5
    ports:
      - 5000:5000/tcp
    environment:
      SEEDS_NUMBER: 9
    volumes:
      - ./seeds.txt:/opt/stellar-faucet/seeds.txt
